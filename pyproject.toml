[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "paper-trump-moral-audiences"
version = "0.1.0"
description = "Reproducible preprocessing and PEW alignment pipeline for Trump moral audience analysis."
readme = "readme.md"
requires-python = ">=3.9"
dependencies = [
  "pypdf>=4.0.0",
]

[tool.setuptools]
py-modules = []
